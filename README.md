# Canalisation

A plant grown from its own chemistry — a real-time simulation of auxin transport
that produces leaf arrangement, vein networks, leaf shapes, flowers and fruit.
Nothing about the plant's shape is drawn.

```bash
node build.js && open canalisation.html
```

Start with [CLAUDE.md](CLAUDE.md).
